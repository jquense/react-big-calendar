import { Canvas } from '@storybook/addon-docs/blocks'
import * as Stories from './elementProps.stories'

# elementProps

- type: `object`

Props passed to main calendar `<div>`.

**Note:** Will not pass down `className`.

<Canvas of={Stories.ElementProps} meta={Stories} />
