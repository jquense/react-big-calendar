import { Canvas } from '@storybook/addon-docs/blocks'
import LinkTo from '@storybook/addon-links/react'
import * as Stories from './formatsDayRangeHeaderFormat.stories'

# formats.dayRangeHeaderFormat

- type: `function (range: Object {start: Date, end: Date}, culture: ?string, localizer: DateLocalizer) => string`

Toolbar header format for the Week views, e.g. "Mar 29 - Apr 04"

<Canvas of={Stories.FormatsDayRangeHeaderFormat} meta={Stories} />
