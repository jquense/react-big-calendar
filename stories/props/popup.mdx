import { Canvas, Story } from '@storybook/addon-docs'
import LinkTo from '@storybook/addon-links/react'

# popup

- type: `boolean`
- default: `true`

Show truncated events in an overlay when you click the "+_x_ more" link.

<Story id="props--popup" />
