import { Canvas, Story } from '@storybook/addon-docs'

# scrollToTime

- type: `instanceOf(Date)`
- default: `new Date()`

Determines how far down the scroll pane is initially scrolled down.

<Story id="props--scroll-to-time" />
