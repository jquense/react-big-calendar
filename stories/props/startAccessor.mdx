import { Canvas, Story } from '@storybook/addon-docs'

# startAccessor

- type: `string | function (event: Object) => Date`
- default: 'start'

The start date/time of the event. Must resolve to a JavaScript `Date` object.

<Canvas>
  <Story id="props--start-accessor" />
</Canvas>
